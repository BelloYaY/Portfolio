export interface Project {
  id: number;
  title: string;
  description: string;
  link: string;
}

export interface NavItem {
  label: string;
  href: string;
}

export interface TrajectoryItem {
  title: string;
  subtitle: string;
  period: string;
  description: string;
}